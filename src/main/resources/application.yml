spring:
  application:
    name: vet-manager-api
  datasource:
    url: jdbc:postgresql://localhost:5432/vet_manager_db
    username: lucas_dev
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true